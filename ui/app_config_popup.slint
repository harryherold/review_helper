import { Popup } from "popup.slint";
import { Style } from "style.slint";
import { SlintReviewHelperConfig, SlintModelUtils } from "globals.slint";
import { ToolButton } from "tool_button.slint";

import {
    VerticalBox,
    LineEdit,
    Button,
    ComboBox,
    Palette,
} from "std-widgets.slint";


export component AppConfigPopup inherits Popup {
    title: "Config";

    VerticalBox {
        alignment: space-between;

        GridLayout {
            spacing: Style.size.spacing;

            Row {
                Text {
                    font-size: Style.font.config-edit;
                    vertical-alignment: center;
                    text: "diff tool";
                }

                ComboBox {
                    private property <int> difftool_idx: SlintReviewHelperConfig.difftool_index;
                    changed difftool_idx => {
                        self.current-index = difftool_idx
                    }
                    current-value: SlintReviewHelperConfig.diff_tool;
                    model: SlintReviewHelperConfig.diff_tool_model;
                    selected(current-value) => {
                        SlintReviewHelperConfig.difftool_index = SlintModelUtils.index_of_string(SlintReviewHelperConfig.diff_tool_model, current-value);
                        SlintReviewHelperConfig.diff_tool = current-value;
                    }
                }

                ToolButton {
                    source: @image-url("../assets/icons/refresh.svg");
                    clicked => {
                        SlintReviewHelperConfig.refresh_diff_tool_model()
                    }
                }
            }

            Row {
                Text {
                    font-size: Style.font.config-edit;
                    vertical-alignment: center;
                    text: "editor";
                }

                editor_edit := LineEdit {
                    height: Style.size.control_height;
                    text <=> SlintReviewHelperConfig.editor;
                }
            }

            Row {
                Text {
                    font-size: Style.font.config-edit;
                    vertical-alignment: center;
                    text: "editor args";
                }

                editor_args_edit := LineEdit {
                    height: Style.size.control_height;
                    text <=> SlintReviewHelperConfig.editor_args;
                }
            }

            Row {
                Text {
                    font-size: Style.font.config-edit;
                    vertical-alignment: center;
                    text: "color theme";
                }

                ComboBox {
                    function to_string(color_scheme: ColorScheme) -> string {
                        return color_scheme == ColorScheme.dark ? "Dark" : "Light";
                    }
                    function to_theme(color_scheme_string: string) -> ColorScheme {
                        return color_scheme_string == to_string(ColorScheme.dark) ? ColorScheme.dark : ColorScheme.light;
                    }

                    model: [to_string(ColorScheme.dark), to_string(ColorScheme.light)];
                    current-index: to_theme(SlintReviewHelperConfig.color_scheme) == ColorScheme.dark ? 0 : 1;
                    current-value: SlintReviewHelperConfig.color_scheme;
                    selected(current-value) => {
                        SlintReviewHelperConfig.color_scheme = current-value;
                        Palette.color-scheme = to_theme(current-value);
                    }
                }
            }
        }

        HorizontalLayout {
            spacing: Style.size.spacing;
            alignment: end;

            Button {
                text: "Cancel";
                clicked => {
                    root.close()
                }
            }

            Button {
                text: "Save";
                clicked => {
                    SlintReviewHelperConfig.save();
                    root.close();
                }
            }
        }
    }
}
