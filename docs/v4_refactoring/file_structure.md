review_helper (in AppData)
    * config.toml (wie bisher)
    * Repo1
    * Repo 2
        * repo.toml (first_commit, base_branch, path)
        * Review1
        * Review2
            * diff.toml (wie bisher bloss ohne path, first_commit, ...)
            * notes.md (wie bisher)