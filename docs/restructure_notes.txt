struct Todos {
	file: PathBuf,
	model: VecModel<TodoItem>,
}

struct Diff {
	start_commit: String
	end_commit: String
	changed_files: VecModel<ChangedFileItem>
}

pub struct Review {
	project_path: PathBuf
	todos: Todos,
	diff: Diff,
}


//////////////////////////////////////////////////////////////


review_datatypes.slint {
	export global RepoReview {
		in property <[NoteItem]> review-todo-item-model;
		in property <string> current-repo;
		pure callback review-open-repo-requested();
	}
	export global Todos {
		in property <string> current-file;
		pure callback review-todos-save-requested();
	}
}

review_todo_view.slint {
}
